{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "deploy": "cd docs/.vitepress/dist && rm -rf .git && git init && git add -A && git commit -m ${VERSION} && git tag ${VERSION} && git remote add origin git@github.com:Jayin/yifujian.cn.git && git push origin ${VERSION} && echo ${VERSION} > version.txt "
  },
  "dependencies": {
    "vue": "^3.5.20"
  },
  "devDependencies": {
    "vitepress": "^1.6"
  }
}
